type Todo {
  id: Int!
  title: String!
  done: Boolean!
}

input InputTodoAll {
  id: Int
  title: String
  done: Boolean
  offset: Int
  limit: Int
}

type ResponseTodoAll {
  todos: [Todo]!
  count: Int!
}

type Query {
  FindTodoAll(cond: InputTodoAll): ResponseTodoAll!
  FindTodo: Todo
}
